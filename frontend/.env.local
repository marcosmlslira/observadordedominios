# Em dev, aponta para localhost para usar o proxy do Next.js (evita CORS)
# O proxy em next.config.js encaminha /v1/* para a API de produção
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_API_URL_PROXY=https://api.observadordedominios.com.br
